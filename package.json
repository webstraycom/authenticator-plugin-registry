{
  "name": "authenticator-plugin-registry",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "compile": "node scripts/compile.js",
    "registry": "node scripts/registry.js",
    "build": "npm run compile && npm run registry && npm run format"
  },
  "dependencies": {
    "esbuild": "^0.28.2",
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2"
  }
}
