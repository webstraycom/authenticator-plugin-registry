{
  "name": "authenticator-plugin-registry",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "registry:build": "node scripts/build.js && npm run format",
    "format": "prettier --write ."
  },
  "dependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2"
  }
}
